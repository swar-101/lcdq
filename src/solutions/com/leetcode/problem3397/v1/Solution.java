package com.leetcode.problem3397.v1;

import java.util.Arrays;

public class Solution {

    public int maxDistinctElements(int[] nums, int k) {
        Arrays.sort(nums);
        int nextFree = Integer.MIN_VALUE;
        int count = 0;
        for (int x : nums) {

        }
    }
}
